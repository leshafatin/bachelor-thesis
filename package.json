{
  "name": "whatever-rendering",
  "version": "1.0.0",
  "description": "Tools to discover what rendering strategy is the best ",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER -c auto \"npm -w client run dev\" \"npm -w server run dev\"",
    "build": "npm -w client run build && npm -w server run build",
    "start": "npm -w server run start",
    "ssg": "npm -w server run ssg"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
